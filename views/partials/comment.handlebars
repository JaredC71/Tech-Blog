<div class="commentContainer">
</div>
<div class="card">
    <div class="card-body">
        <p class="comment">
            {{ comment_text }}
        </p>
        <div class="card-footer commentOrigin">
            {{ user.username }}, {{ format_date created_at }}
        </div>
    </div>
    {{/each}}
</div>